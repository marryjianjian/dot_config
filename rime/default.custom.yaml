patch:
  schema_list:
    - schema: luna_pinyin_simp
    - schema: luna_pinyin
    - schema: terra_pinyin

  "key_binder/bindings":
    - { when: paging, accept: bracketleft, send: Page_Up }
    - { when: has_menu, accept: bracketright, send: Page_Down }

  "menu/page_size": 9

  "switcher/hotkeys":
    - "Control+grave"

  # set in build/ibus_rime.yaml
  # style:
  #   horizontal: true

